# 🚀 自动开发服务器启动工具 v1.0.0 - 正式版

## 📦 安装包信息

**文件名**: `自动开发服务器启动工具 Setup 1.0.0.exe`  
**文件大小**: 约 92.4 MB  
**位置**: `release-final/自动开发服务器启动工具 Setup 1.0.0.exe`  
**架构**: x64  
**发布日期**: 2026-01-29

## ✨ 核心功能

### 1. 智能项目检测
- ✅ 自动读取 `package.json`
- ✅ 智能识别启动命令（dev > start > serve）
- ✅ 自动检测包管理器（pnpm > yarn > npm）
- ✅ 无需手动创建配置文件

### 2. 项目管理
- ✅ 项目历史记录
- ✅ 一键启动/停止服务器
- ✅ 实时服务器输出日志
- ✅ 状态可视化（未启动、启动中、运行中、已停止、错误）
- ✅ 在文件管理器中打开项目
- ✅ 从历史记录中删除项目

### 3. 用户体验
- ✅ 柔和的深色主题界面
- ✅ 响应式自适应布局
- ✅ 超长项目名称自动省略显示
- ✅ URL 可点击直接在浏览器中打开
- ✅ 无菜单栏设计（简洁界面）
- ✅ 窗口设置持久化

### 4. 进程管理
- ✅ Windows 进程树完整停止（使用 taskkill /T /F）
- ✅ 应用退出时自动停止所有服务器
- ✅ 多项目并行运行支持
- ✅ 实时输出日志（最多保留 100 行）

## 🎨 界面设计

### 柔和终端美学
- **配色方案**: 深蓝背景 + 柔和蓝绿强调色
- **字体**: JetBrains Mono（等宽）+ Space Grotesk（标题）
- **效果**: 微妙的 CRT 扫描线 + 网格背景
- **交互**: 柔和的发光效果 + 平滑过渡动画

### 响应式布局
- **大屏幕（> 1200px）**: 双列布局（项目列表 + 输出日志）
- **中屏幕（< 1200px）**: 单列布局，项目列表限高 40vh
- **小屏幕（< 768px）**: 紧凑布局，更小的字体和间距

### 超长名称处理
- 项目名称超长时显示省略号（...）
- 路径超长时显示省略号
- 悬停可查看完整内容（通过浏览器原生 tooltip）

## 📋 安装说明

### 方式 1: 使用安装程序（推荐）
1. 双击运行 `自动开发服务器启动工具 Setup 1.0.0.exe`
2. 选择安装位置（可自定义）
3. 选择是否创建桌面快捷方式
4. 点击"安装"按钮
5. 安装完成后可直接启动

### 方式 2: 使用便携版
1. 解压 `win-unpacked` 文件夹到任意位置
2. 运行 `自动开发服务器启动工具.exe`
3. 无需安装，可直接使用

## 🚀 使用方法

### 快速开始
1. 启动应用
2. 点击"📁 选择项目"按钮
3. 选择包含 `package.json` 的项目文件夹
4. 应用自动检测配置并添加到历史
5. 点击"▶ 启动"按钮启动项目
6. 在右侧面板查看实时输出日志
7. 点击输出中的 URL 可直接在浏览器中打开
8. 点击"⏹ 停止"按钮停止项目

### 自动检测规则

#### 启动命令优先级
1. `dev` - 如果 package.json 中有 `"dev": "..."` 脚本
2. `start` - 如果有 `"start": "..."` 脚本
3. `serve` - 如果有 `"serve": "..."` 脚本

#### 包管理器检测
- 存在 `pnpm-lock.yaml` → 使用 **pnpm**
- 存在 `yarn.lock` → 使用 **yarn**
- 否则 → 使用 **npm**

## 📊 支持的项目类型

- ✅ Vite 项目
- ✅ Vue CLI 项目
- ✅ React (Create React App)
- ✅ Next.js 项目
- ✅ Nuxt.js 项目
- ✅ Express 服务器
- ✅ 任何包含 package.json 的 Node.js 项目

## ⚠️ 系统要求

- **操作系统**: Windows 10/11 (x64)
- **Node.js**: 建议安装 Node.js 16+ (用于运行项目)
- **包管理器**: npm/yarn/pnpm (至少安装一个)
- **内存**: 建议 4GB 以上
- **磁盘空间**: 安装需要约 200MB

## 🐛 已修复的问题

### v1.0.0 修复列表
1. ✅ 界面空白问题（preload 脚本依赖 + 缺少函数）
2. ✅ 配置文件强制要求（现在自动检测 package.json）
3. ✅ 输出日志乱码（移除 ANSI 颜色代码）
4. ✅ 停止按钮无效（使用 taskkill /T /F）
5. ✅ 顶部菜单栏显示（完全移除）
6. ✅ URL 无法点击（自动转换为可点击链接）
7. ✅ 超长项目名称显示问题（自动省略）
8. ✅ 响应式布局问题（支持多种屏幕尺寸）

## 🎯 常见问题

### Q: 为什么提示"未找到 package.json"？
A: 请确保选择的文件夹是项目根目录，且包含 `package.json` 文件。

### Q: 启动失败怎么办？
A: 
1. 检查 package.json 中是否有启动脚本（dev/start/serve）
2. 确保已安装项目依赖（运行 `npm install`）
3. 查看右侧输出面板的错误信息
4. 确保系统已安装对应的包管理器

### Q: 可以同时启动多个项目吗？
A: 可以！每个项目都是独立的进程，互不影响。

### Q: 关闭应用后服务器会停止吗？
A: 会的。应用关闭时会自动停止所有正在运行的服务器。

### Q: 如何卸载？
A: 
- 如果使用安装程序安装：在 Windows 设置 > 应用 > 应用和功能 中卸载
- 如果使用便携版：直接删除文件夹即可

### Q: 为什么输出日志只显示最后 100 行？
A: 为了性能考虑，应用只保留最近 100 行输出。如需查看完整日志，请在项目文件夹中查看。

## 📝 更新日志

### v1.0.0 (2026-01-29)
- 🎉 首次正式发布
- ✨ 实现自动配置检测
- 🎨 柔和终端美学设计
- 📱 响应式自适应布局
- 🔧 超长项目名称处理
- 🌐 URL 可点击功能
- 🐛 修复所有已知问题

## 🔮 未来计划

- [ ] 添加亮色主题选项
- [ ] 支持自定义配色方案
- [ ] 添加项目分组功能
- [ ] 支持多语言（英文/中文切换）
- [ ] 添加启动动画
- [ ] 支持导入/导出项目配置
- [ ] 添加项目搜索功能
- [ ] 支持自定义启动命令

## 📄 许可证

MIT License

## 🙏 致谢

- **Electron** - 跨平台桌面应用框架
- **React** - UI 框架
- **Vite** - 构建工具
- **JetBrains Mono** - 等宽字体
- **Space Grotesk** - 标题字体
- **Anthropic Agent Skills** - frontend-design 技能

---

**开发者**: 使用 Kiro AI 辅助开发  
**技术栈**: Electron + React + TypeScript + Vite  
**设计工具**: Anthropic Agent Skills - frontend-design  
**版本**: v1.0.0  
**发布日期**: 2026-01-29

如有问题或建议，欢迎反馈！
