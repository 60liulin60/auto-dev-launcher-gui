{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop capability set for the launcher UI.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
