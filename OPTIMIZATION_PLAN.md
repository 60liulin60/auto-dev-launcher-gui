# 项目优化实施计划 (Optimization Plan)

本文档概述了 `auto-dev-launcher-gui` 项目的后续优化方向，旨在提升系统的稳定性、性能及用户体验。

---

## 🚀 实施优先级摘要

| 优先级 | 优化维度 | 核心目标 | 预计难度 |
| :--- | :--- | :--- | :--- |
| **P0** | 稳定性与性能 | 解决高频日志卡顿与数据损坏风险 | ⭐⭐ |
| **P1** | 核心功能增强 | 提升从启动到访问的自动化链路 | ⭐⭐⭐ |
| **P2** | 用户体验 (UX) | 增强后台管理能力与日志分析深度 | ⭐⭐ |

---

## 📅 详细计划

### 第一阶段：核心加固 (P0 - 紧急且重要)

#### 1.1 日志渲染性能优化 (渲染进程)
- **问题**：大量日志输出（如安装依赖）会导致 React 状态更新过快，引发 UI 假死。
- **方案**：
  - 引入 `react-window` 或 `virtuoso` 实现 **虚拟滚动**。
  - 在 `App.tsx` 中增加 **RequestAnimationFrame (rAF) 节流更新**。
- **验收标准**：在安装 1000+ 依赖包时，界面滑动保持 60fps。

#### 1.2 原子化存储写入 (主进程)
- **问题**：`fs.writeFileSync` 存在非原子性风险，损坏后无法恢复。
- **方案**：
  - 集成 `write-file-atomic` 库。
  - 增加 `.bak` 自动备份与检测回滚机制。
- **验收标准**：模拟写入时断电，重启后能通过备份文件恢复项目历史。

---

### 第二阶段：链路优化 (P1 - 重要)

#### 2.1 智能端口探测与快速访问
- **问题**：用户需手动在控制台寻找端口。
- **方案**：
  - 主进程 `ProcessManager` 增加正则监听器，识别 `Local: http://localhost:(\d+)`。
  - 渲染进程在项目卡片同步显示 `[点击打开]` 按钮。
- **验收标准**：项目启动后 2s 内，界面出现可点击的访问链接。

#### 2.2 环境变量白名单防护
- **问题**：用户环境变量可能污染系统关键变量。
- **方案**：
  - 建立环境变量过滤层，禁止覆盖 `PATH`、`NODE_OPTIONS` 等。
- **验收标准**：在配置中尝试覆盖 `PATH`，系统能识别并拦截。

---

### 第三阶段：体验增强 (P2 - 提升)

#### 3.1 系统托盘 (Tray) 集成
- **方案**：
  - 增加托盘图标，支持最小化到托盘。
  - 托盘右键菜单支持：`全部停止`、`显示窗口`、`退出应用`。

#### 3.2 高级日志筛选
- **方案**：
  - 增加 `Only Error` 切换开关。
  - 实现日志关键字全局高亮搜索。

---

## 📈 维护说明
- 每项优化实施后，需更新 `CHANGELOG.md`。
- 重大底层变动（如存储格式变化）需执行版本迁移逻辑。
